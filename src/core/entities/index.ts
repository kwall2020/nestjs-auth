import { Category } from './category.entity';
import { Transaction } from './transaction.entity';

export const entities: any[] = [Category, Transaction];

export * from './category.entity';
export * from './transaction.entity';
